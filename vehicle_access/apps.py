from django.apps import AppConfig


class VehicleAccessConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "vehicle_access"
    verbose_name = "Acceso de vehículos"
